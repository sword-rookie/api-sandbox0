services:
  orchestrator:
    build: ./orchestrator
    ports:
      - "8081:8080"
    volumes:
      - ./volumes:/volumes
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PORT=8080
    depends_on:
      - ollama

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama

volumes:
  ollama: